This rails app will run with the following commands:

 bundle install
 rake db:create
 rake db:migrate
 rake db:seed
 rails server

Rails version 4.2.1

 Note:
-In order to test prediction the data base should at least be run 10 minutes
to have data to perform its predictions.
